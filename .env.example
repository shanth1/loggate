# .env.example - Central Configuration File
# -----------------------------------------------------------------------------
# Copy this file to .env and customize it for your environment.
# This file is loaded by Docker Compose on startup.

# --- Grafana Admin Credentials ---
# IMPORTANT: Change this password for any production or public-facing instance!
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# --- Port Binding Configuration ---
# Controls which IP address the services bind to on the host machine.
# - For local development (max accessibility): Use 0.0.0.0 or comment out the line.
# - For production (max security): Use 127.0.0.1 to prevent external access, assuming
#   you have a reverse proxy (like Nginx) handling public traffic to Grafana.
DOCKER_HOST_BIND_IP=0.0.0.0

# --- Resource Limits Configuration ---
# By default, all limits are commented out, meaning services run with unlimited resources.
# To enable a limit for a specific service in production, uncomment the corresponding lines.
#
# CPU values are in fractions of a CPU core (e.g., "0.5" is half a core).
# Memory values use units like K, M, G (e.g., "512M" is 512 Megabytes).

# --- LogGate ---
# LOGGATE_LIMIT_CPUS=0.5
# LOGGATE_LIMIT_MEM=256M
# LOGGATE_RESERVE_CPUS=0.2
# LOGGATE_RESERVE_MEM=128M

# --- Prometheus ---
# PROMETHEUS_LIMIT_CPUS=1.0
# PROMETHEUS_LIMIT_MEM=1G
# PROMETHEUS_RESERVE_CPUS=0.5
# PROMETHEUS_RESERVE_MEM=512M

# --- Loki ---
# LOKI_LIMIT_CPUS=1.0
# LOKI_LIMIT_MEM=1G
# LOKI_RESERVE_CPUS=0.5
# LOKI_RESERVE_MEM=512M

# --- Grafana ---
# GRAFANA_LIMIT_CPUS=0.5
# GRAFANA_LIMIT_MEM=512M
# GRAFANA_RESERVE_CPUS=0.2
# GRAFANA_RESERVE_MEM=256M

# --- Promtail ---
# PROMTAIL_LIMIT_CPUS=0.2
# PROMTAIL_LIMIT_MEM=128M
# PROMTAIL_RESERVE_CPUS=0.1
# PROMTAIL_RESERVE_MEM=64M

# --- cAdvisor ---
# CADVISOR_LIMIT_CPUS=0.5
# CADVISOR_LIMIT_MEM=256M
# CADVISOR_RESERVE_CPUS=0.2
# CADVISOR_RESERVE_MEM=128M
